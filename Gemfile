source "https://rubygems.org"

# For using GitHub Pages, it is better to let the github-pages gem
# manage the dependencies and versions for jekyll and other gems
# Fix the version to 232 which is the current version of github-pages on the server
gem "github-pages", "~> 232", group: :jekyll_plugins

